[
    {"type": "product", "question": "how does an incoming web request find and run the right handler function", "expect": ["src/flask/app.py"]},
    {"type": "product", "question": "how does a visitor stay signed in between page loads", "expect": ["src/flask/sessions.py"]},
    {"type": "product", "question": "how are settings loaded from environment variables or a settings file", "expect": ["src/flask/config.py"]},
    {"type": "product", "question": "how is an html page produced from a page skeleton with placeholders", "expect": ["src/flask/templating.py"]},
    {"type": "product", "question": "how can a large project split its url routes into separate reusable modules", "expect": ["src/flask/blueprints.py", "src/flask/sansio/blueprints.py"]},
    {"type": "product", "question": "how does the command line tool find the application and start the development server", "expect": ["src/flask/cli.py"]},
    {"type": "product", "question": "how are json responses generated and how can their formatting be customized", "expect": ["src/flask/json/provider.py", "src/flask/json/__init__.py"]},
    {"type": "product", "question": "what keeps track of the request currently being handled", "expect": ["src/flask/ctx.py", "src/flask/globals.py"]},
    {"type": "product", "question": "how can a developer simulate a request to the application without running a server", "expect": ["src/flask/testing.py"]},
    {"type": "product", "question": "how is a file from disk sent back to the browser as a download", "expect": ["src/flask/helpers.py"]},
    {"type": "product", "question": "how can other parts of the application be notified when a request finishes or errors", "expect": ["src/flask/signals.py"]},
    {"type": "product", "question": "how are incoming requests and outgoing responses represented as objects", "expect": ["src/flask/wrappers.py"]},
    {"type": "product", "question": "how can a class with get and post methods serve a url instead of a plain function", "expect": ["src/flask/views.py"]},
    {"type": "product", "question": "what makes the currently running application accessible from anywhere during a request", "expect": ["src/flask/globals.py", "src/flask/ctx.py"]},
    {"type": "product", "question": "how does the framework clean up resources once a request is finished", "expect": ["src/flask/ctx.py", "src/flask/app.py"]},
    {"type": "product", "question": "how are images styles and scripts served from a folder of unchanging assets", "expect": ["src/flask/helpers.py", "src/flask/app.py"]},
    {"type": "product", "question": "how does the application record diagnostic messages while running", "expect": ["src/flask/logging.py"]},
    {"type": "product", "question": "what happens when an unhandled error occurs while serving a request", "expect": ["src/flask/app.py"]},
    {"type": "product", "question": "how can a response be streamed in pieces while request information stays available", "expect": ["src/flask/helpers.py"]},
    {"type": "product", "question": "how does a handler send the browser to a different page address", "expect": ["src/flask/helpers.py", "src/flask/sansio/app.py"]},

    {"type": "identifier", "question": "where is SecureCookieSessionInterface defined", "expect": ["src/flask/sessions.py"]},
    {"type": "identifier", "question": "where is full_dispatch_request implemented", "expect": ["src/flask/app.py"]},
    {"type": "identifier", "question": "where is wsgi_app defined", "expect": ["src/flask/app.py"]},
    {"type": "identifier", "question": "where is the url_for helper function implemented", "expect": ["src/flask/helpers.py", "src/flask/app.py"]},
    {"type": "identifier", "question": "where is send_file implemented", "expect": ["src/flask/helpers.py"]},
    {"type": "identifier", "question": "where is stream_with_context defined", "expect": ["src/flask/helpers.py"]},
    {"type": "identifier", "question": "where is TaggedJSONSerializer defined", "expect": ["src/flask/json/tag.py"]},
    {"type": "identifier", "question": "where is DefaultJSONProvider implemented", "expect": ["src/flask/json/provider.py"]},
    {"type": "identifier", "question": "where is DispatchingJinjaLoader defined", "expect": ["src/flask/templating.py"]},
    {"type": "identifier", "question": "where is ScriptInfo defined", "expect": ["src/flask/cli.py"]},
    {"type": "identifier", "question": "where is MethodView implemented", "expect": ["src/flask/views.py"]},
    {"type": "identifier", "question": "where is EnvironBuilder subclassed for the framework test client", "expect": ["src/flask/testing.py"]},
    {"type": "identifier", "question": "where is from_prefixed_env implemented", "expect": ["src/flask/config.py"]},
    {"type": "identifier", "question": "where is got_request_exception defined", "expect": ["src/flask/signals.py"]},
    {"type": "identifier", "question": "where is has_request_context defined", "expect": ["src/flask/ctx.py"]},

    {"type": "leaky", "question": "How do blueprints get registered on the application?", "expect": ["src/flask/blueprints.py", "src/flask/sansio/blueprints.py"]},
    {"type": "leaky", "question": "Where is the config object that loads application settings?", "expect": ["src/flask/config.py"]},
    {"type": "leaky", "question": "How does the cli locate the app object to run commands against?", "expect": ["src/flask/cli.py"]},
    {"type": "leaky", "question": "Where are the request and response wrappers defined?", "expect": ["src/flask/wrappers.py"]},
    {"type": "leaky", "question": "Where are globals like current_app and request set up?", "expect": ["src/flask/globals.py"]},
    {"type": "leaky", "question": "How does templating wire the jinja environment to the application?", "expect": ["src/flask/templating.py"]},

    {"type": "integration", "question": "how do flask and werkzeug work together to handle a request", "expect": ["src/flask/app.py", "src/flask/wrappers.py"], "manifest": "pyproject.toml"},
    {"type": "integration", "question": "how do flask and jinja2 work together to render pages", "expect": ["src/flask/templating.py"], "manifest": "pyproject.toml"},
    {"type": "integration", "question": "how does flask integrate with click for its command line interface", "expect": ["src/flask/cli.py"], "manifest": "pyproject.toml"},
    {"type": "integration", "question": "how does flask use itsdangerous to sign the session cookie", "expect": ["src/flask/sessions.py"], "manifest": "pyproject.toml"},
    {"type": "integration", "question": "how does flask use blinker to provide signals", "expect": ["src/flask/signals.py"], "manifest": "pyproject.toml"}
]
